services:
  app:
    build:
      context: .
    volumes:
      - ./config/config.json:/app/config/config.json
    env_file:
      - ./config/.env
    expose:
      - "5000"
    restart: unless-stopped